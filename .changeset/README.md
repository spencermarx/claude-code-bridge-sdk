# Changesets

This directory holds changesets — small markdown files that describe pending changes for the next release.

To add one:

```bash
pnpm changeset
```

Choose the affected package(s), the semver bump, and write a short summary. Commit the file alongside your PR.

See [the changesets docs](https://github.com/changesets/changesets) for details.
